<div class="mt-8">
    <div class="flex justify-end mb-4">
        <span class="text-gray-400 text-sm bg-[#14254E] px-4 py-2 rounded-md">
            <?= $paginator->entriesInfo() ?>
        </span>
    </div>

    <nav aria-label="Page navigation">
        <ul class="flex justify-end items-center space-x-1">
            <?php if ($paginator->hasPreviousPage()) : ?>
                <li>
                    <a class="px-4 py-2 bg-[#14254E] text-[#4A90E2] rounded-md hover:bg-[#2B6CE4] hover:text-white transition-colors border border-[#2B6CE4] font-medium"
                       href="<?= route($paginator->getRouteName(), ['page' => $paginator->previousPage()]) ?>">
                        Anterior
                    </a>
                </li>
            <?php else : ?>
                <li>
                    <span class="px-4 py-2 bg-[#0f1f3d] text-gray-600 rounded-md border border-[#1a2a4e] cursor-not-allowed font-medium">
                        Anterior
                    </span>
                </li>
            <?php endif; ?>

            <?php for ($page = 1; $page <= $paginator->totalOfPages(); $page++) : ?>
                <li>
                    <?php if ($paginator->isPage($page)) : ?>
                        <span class="px-4 py-2 bg-[#2B6CE4] text-white rounded-md border border-[#2B6CE4] font-semibold">
                            <?= $page ?>
                        </span>
                    <?php else : ?>
                        <a class="px-4 py-2 bg-[#14254E] text-[#4A90E2] rounded-md hover:bg-[#2B6CE4] hover:text-white transition-colors border border-[#2B6CE4] font-medium"
                           href="<?= route($paginator->getRouteName(), ['page' => $page]) ?>">
                            <?= $page ?>
                        </a>
                    <?php endif; ?>
                </li>
            <?php endfor; ?>
            <?php if ($paginator->hasNextPage()) : ?>
                <li>
                    <a class="px-4 py-2 bg-[#14254E] text-[#4A90E2] rounded-md hover:bg-[#2B6CE4] hover:text-white transition-colors border border-[#2B6CE4] font-medium"
                       href="<?= route($paginator->getRouteName(), ['page' => $paginator->nextPage()]) ?>">
                        Próxima
                    </a>
                </li>
            <?php else : ?>
                <li>
                    <span class="px-4 py-2 bg-[#0f1f3d] text-gray-600 rounded-md border border-[#1a2a4e] cursor-not-allowed font-medium">
                        Próxima
                    </span>
                </li>
            <?php endif; ?>
        </ul>
    </nav>
</div>