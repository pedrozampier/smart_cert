<div class="max-w-6xl mx-auto">
    <header class="mb-8">
        <h1 class="text-[#4A90E2] text-3xl font-semibold">Detalhes do Evento</h1>
        <p class="text-gray-400 mt-2">Informações completas do evento</p>
    </header>

    <div class="bg-[#14254E] rounded-lg border border-[#2B6CE4] overflow-hidden">
        <div class="p-6">
            <div class="mb-6">
                <h2 class="text-2xl font-semibold text-white"><?= $event->name ?></h2>
                <div class="w-16 h-1 bg-[#2B6CE4] mt-2 rounded-full"></div>
            </div>

            <?php if ($event->logo_id && $event->logo) : ?>
                <div class="mb-6">
                    <label class="block text-sm font-medium text-[#4A90E2] mb-3">Logo do Evento:</label>
                    <div class="flex justify-center items-center bg-white rounded-lg p-4 border border-[#2B6CE4]">
                        <img src="<?= $event->logo->file_path ?>" alt="<?= $event->logo->name ?>" class="max-h-32 object-contain">
                    </div>
                </div>
            <?php endif; ?>

            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6 mb-6">
                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">ID:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            #<?= $event->id ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Nome do Evento:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->name ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Tipo de Evento:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->event_type ?? '-' ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Carga Horária:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->workload_hours ? $event->workload_hours . ' horas' : '-' ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Data de Início:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->start_date ? date('d/m/Y', strtotime($event->start_date)) : '-' ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Data de Término:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->end_date ? date('d/m/Y', strtotime($event->end_date)) : '-' ?>
                        </p>
                    </div>
                </div>

                <div class="space-y-4">
                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Local:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->event_location ?? '-' ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Criado por:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->creator->name ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Total de Participantes:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->participants()->count() ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Data de Criação:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->created_at ? date('d/m/Y H:i', strtotime($event->created_at)) : '-' ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Última Atualização:</label>
                        <p class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->updated_at ? date('d/m/Y H:i', strtotime($event->updated_at)) : '-' ?>
                        </p>
                    </div>

                    <div>
                        <label class="block text-sm font-medium text-[#4A90E2] mb-1">Status:</label>
                        <div class="bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e]">
                            <?= $event->is_active
                                ? '<span class="bg-green-600 text-white px-3 py-1 rounded-full text-xs font-medium">Ativo</span>'
                                : '<span class="bg-red-600 text-white px-3 py-1 rounded-full text-xs font-medium">Inativo</span>'
                            ?>
                        </div>
                    </div>
                </div>
            </div>

            <?php if ($event->description) : ?>
                <div class="mb-6">
                    <label class="block text-sm font-medium text-[#4A90E2] mb-1">Descrição:</label>
                    <div class="text-white bg-[#0B1636] px-4 py-3 rounded-md border border-[#1a2a4e] whitespace-pre-wrap">
                        <?= $event->description ?>
                    </div>
                </div>
            <?php endif; ?>

            <?php if ($event->participants()->count() > 0) : ?>
                <div class="mb-6">
                    <label class="block text-sm font-medium text-[#4A90E2] mb-3">Participantes Inscritos:</label>
                    <div class="bg-[#0B1636] rounded-md border border-[#1a2a4e] overflow-hidden">
                        <table class="w-full">
                            <thead class="bg-[#14254E] border-b border-[#2B6CE4]">
                                <tr>
                                    <th class="text-left py-3 px-4 text-[#4A90E2] font-semibold text-sm">ID</th>
                                    <th class="text-left py-3 px-4 text-[#4A90E2] font-semibold text-sm">Nome</th>
                                    <th class="text-left py-3 px-4 text-[#4A90E2] font-semibold text-sm">Email</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-[#1a2a4e]">
                                <?php foreach ($event->participants as $participant) : ?>
                                    <tr class="hover:bg-[#14254E] transition-colors">
                                        <td class="py-3 px-4 text-white">#<?= $participant->id ?></td>
                                        <td class="py-3 px-4 text-white"><?= $participant->name ?></td>
                                        <td class="py-3 px-4 text-white"><?= $participant->email ?></td>
                                    </tr>
                                <?php endforeach; ?>
                            </tbody>
                        </table>
                    </div>
                </div>
            <?php endif; ?>

            <div class="border-t border-[#2B6CE4] my-6"></div>

            <div class="flex flex-col sm:flex-row gap-4 justify-between">
                <form action="<?= route('events.destroy', ['id' => $event->id]) ?>" method="POST" onsubmit="return confirm('Tem certeza que deseja remover este evento? Todos os participantes serão desvinculados.')">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit"
                            class="px-6 py-3 bg-red-600 text-white rounded-md hover:bg-red-700 transition-colors font-medium">
                        <i class="bi bi-trash mr-2"></i>
                        Remover Evento
                    </button>
                </form>

                <div class="flex flex-col sm:flex-row gap-4">
                    <a href="<?= route('events.index') ?>"
                       class="px-6 py-3 border border-[#2B6CE4] text-[#2B6CE4] rounded-md hover:bg-[#2B6CE4] hover:text-white transition-colors font-medium text-center">
                        Voltar
                    </a>
                    <a href="<?= route('event.participants.manage', ['id' => $event->id]) ?>"
                       class="px-6 py-3 bg-green-600 text-white rounded-md hover:bg-green-700 transition-colors font-medium text-center">
                        <i class="bi bi-people mr-2"></i>
                        Gerenciar Participantes
                    </a>
                    <a href="<?= route('events.edit', ['id' => $event->id]) ?>"
                       class="px-6 py-3 bg-[#2B6CE4] text-white rounded-md hover:bg-[#1F54C7] transition-colors font-medium text-center">
                        <i class="bi bi-pencil-square mr-2"></i>
                        Editar Evento
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
