<div class="max-w-7xl mx-auto">
    <header class="mb-8">
        <h1 class="text-[#4A90E2] text-3xl font-semibold">
            <?= $title ?>
        </h1>
    </header>

    <div class="flex justify-end mb-8">
        <a href="<?= route('event.participants') ?>" class="bg-[#2B6CE4] text-white px-6 py-3 rounded-md hover:bg-[#1F54C7] transition-colors font-medium inline-flex items-center gap-2">
            <i class="bi bi-calendar-plus"></i>
            Procurar Eventos
        </a>
    </div>

    <?php if (empty($events)) : ?>
        <div class="bg-[#14254E] border border-[#2B6CE4] rounded-lg p-8 text-center">
            <i class="bi bi-calendar-x text-[#4A90E2] text-5xl mb-4"></i>
            <p class="text-white text-lg mb-4">Você ainda não está inscrito em nenhum evento.</p>
            <a href="<?= route('event.participants') ?>" class="bg-[#2B6CE4] text-white px-6 py-3 rounded-md hover:bg-[#1F54C7] transition-colors font-medium inline-flex items-center gap-2">
                <i class="bi bi-search"></i>
                Explorar Eventos
            </a>
        </div>
    <?php else : ?>
        <div class="rounded-lg overflow-hidden border border-[#2B6CE4]">
            <table class="w-full">
                <thead class="bg-[#14254E]">
                    <tr>
                        <th class="text-left py-4 px-6 text-[#4A90E2] font-semibold text-sm">ID</th>
                        <th class="text-left py-4 px-6 text-[#4A90E2] font-semibold text-sm">Nome do Evento</th>
                        <th class="text-left py-4 px-6 text-[#4A90E2] font-semibold text-sm">Data Início</th>
                        <th class="text-left py-4 px-6 text-[#4A90E2] font-semibold text-sm">Data Fim</th>
                        <th class="text-left py-4 px-6 text-[#4A90E2] font-semibold text-sm">Local</th>
                        <th class="text-left py-4 px-6 text-[#4A90E2] font-semibold text-sm">Carga Horária</th>
                        <th class="text-left py-4 px-6 text-[#4A90E2] font-semibold text-sm">Criado por</th>
                    </tr>
                </thead>
                <tbody class="bg-[#0B1636] divide-y divide-[#14254E]">
                    <?php foreach ($events as $event) : ?>
                        <tr class="hover:bg-[#14254E] transition-colors">
                            <td class="py-4 px-6 text-[#4A90E2]">
                                #<?= $event->id ?>
                            </td>
                            <td class="py-4 px-6 text-white font-medium"><?= $event->name ?></td>
                            <td class="py-4 px-6 text-white">
                                <?= $event->start_date ? date('d/m/Y', strtotime($event->start_date)) : '-' ?>
                            </td>
                            <td class="py-4 px-6 text-white">
                                <?= $event->end_date ? date('d/m/Y', strtotime($event->end_date)) : '-' ?>
                            </td>
                            <td class="py-4 px-6 text-white">
                                <?= $event->event_location ?? '-' ?>
                            </td>
                            <td class="py-4 px-6 text-white">
                                <?= $event->workload_hours ? $event->workload_hours . 'h' : '-' ?>
                            </td>
                            <td class="py-4 px-6 text-white"><?= $event->creator->name ?></td>
                        </tr>
                    <?php endforeach; ?>
                </tbody>
            </table>
        </div>
    <?php endif; ?>
</div>
